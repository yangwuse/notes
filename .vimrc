" 语法高亮
syntax on

" 显示行号
set number

" 支持鼠标操作
set mouse=a

" 使用utf-8编码
set encoding=utf-8

" 使用256色
set t_Co=256

" 根据文件类型缩进
filetype indent on

" 换行后对齐上行
set autoindent

" tab空格数
set tabstop=4

" >>或<<或==缩进字符数
set shiftwidth=4

" 光标所在的当前行高亮
set cursorline

" 关闭自动折行
set nowrap

" 垂直滚动时，光标距离顶部/底部的位置（单位：行）
set scrolloff=10

" 水平移动时光标距离行首或行尾的位置（单位：字符）
set sidescrolloff=15

" 在状态栏显示光标的当前位置（位于哪一行哪一列）
set ruler

" 光标遇到圆括号、方括号、大括号时，自动高亮对应的另一个圆括号、方括号和大括号
set showmatch

" 搜索时, 高亮显示匹配结果
set hlsearch

" 搜索时, 每输入一个字符，就自动跳到第一个匹配的结果
set incsearch

" 搜索时忽略大小写
set ignorecase

" 打开英语单词的拼写检查
set spelllang=en_us
